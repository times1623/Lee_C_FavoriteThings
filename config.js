const config = {
  user: "root",
  password: "root", // for window users leave this blank
  host: "localhost",
  port: "8889", // window users - sql port is 3306
  database: "marvelcharacters"
}

module.exports = config;
